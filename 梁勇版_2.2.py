from math import *
radius,length = eval(input("enter the radius and length of a cylinder:"))
area = (radius ** 2) * pi
volume = area * length
print("the area is {}, \
the volume is{}".format(area,volume))


